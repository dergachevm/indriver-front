export enum Url {
    GetVacancies = "api/vacancies",
    GetVacancy = "api/vacancies/",
    SubmitVacancyResponse = "api/submit-vacancy-response",
}

export enum StatusCode {
    Ok = 200,
    BadRequest = 400,
    InternalServerError = 500,
}
