import { THeaderColorClassRecord } from "./types";
import styles from "./styles.module.scss";

export const headerColorClassRecord: THeaderColorClassRecord = {
    bezh: styles.bezh,
    dark: styles.dark,
    green: styles.green,
    light: styles.light,
    transparent: styles.transparent,
    white: styles.white,
};
