import { FC } from "react";

export type TCultureOfChallengeProps = {
    scrollStart: number;
    scrollEnd: number;
    number: number;
};

export type TCultureOfChallengeComponent = FC<TCultureOfChallengeProps>;
